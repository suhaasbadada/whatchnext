import { Routes } from '@angular/router';
import { SwipeContainerComponent } from './components/swipe-container/swipe-container.component';

export const routes: Routes = [
  { path: '', component: SwipeContainerComponent },
];
